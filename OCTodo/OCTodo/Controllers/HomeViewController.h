/**
 * HomeViewController.h — 首页控制器声明
 *
 * 【概念说明】
 * UIViewController 是 iOS 里最核心的概念之一
 * 一个 ViewController = 一个"页面"（类似 Web 的一个路由页面/React 组件）
 *
 * 每个页面负责：
 * 1. 管理一个 view（视图）— 就是页面的 UI
 * 2. 处理用户交互 — 按钮点击、列表滑动等
 * 3. 协调数据和 UI — 从模型拿数据，显示到视图上
 */

#import <UIKit/UIKit.h>

@interface HomeViewController : UIViewController

@end
